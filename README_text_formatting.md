
# Text Formatting Extensions

!!! danger "Attention"

    Default strong/em from python markdown are disabled by this extension

del: -
:   you can use single or double - to create an del tag
:   single mode (-) cannot have the char - inside
:   double mode (--) can have single - inside
:   word mode cannot have single - inside nor space

code: `
:   you can use single or double ` to create an code tag
:   single mode (`) cannot have the char ` inside
:   double mode (``) can have single ` inside
:   word mode cannot have single ` inside nor space

u: ^
:   you can use single or double ^ to create an u tag
:   single mode (^) cannot have the char ^ inside
:   double mode (^^) can have single ^ inside
:   word mode cannot have single ^ inside nor space

s: ~
:   you can use single or double ~ to create an s tag
:   single mode (~) cannot have the char ~ inside
:   double mode (~~) can have single ~ inside
:   word mode cannot have single ~ inside nor space

mark: =
:   you can use single or double = to create an mark tag
:   single mode (=) cannot have the char = inside
:   double mode (==) can have single = inside
:   word mode cannot have single = inside nor space

ins: +
:   you can use single or double + to create an ins tag
:   single mode (+) cannot have the char + inside
:   double mode (++) can have single + inside
:   word mode cannot have single + inside nor space

em: _
:   you can use single or double _ to create an em tag
:   single mode (_) cannot have the char _ inside
:   double mode (__) can have single _ inside
:   word mode cannot have single _ inside nor space

strong: *
:   you can use single or double * to create an strong tag
:   single mode (*) cannot have the char * inside
:   double mode (**) can have single * inside
:   word mode cannot have single * inside nor space


> generated by C:/Users/glutz/Workspace/chords/tests/text_formatting/tags.sh
