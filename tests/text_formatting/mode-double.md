options: { 'mode': 'double' }
--here we can use - after - with ease, but can be for -words- only--
``here we can use ` after ` with ease, but can be for `words` only``
^^here we can use ^ after ^ with ease, but can be for ^words^ only^^
~~here we can use ~ after ~ with ease, but can be for ~words~ only~~
==here we can use = after = with ease, but can be for =words= only==
++here we can use + after + with ease, but can be for +words+ only++
__here we can use _ after _ with ease, but can be for _words_ only__
**here we can use * after * with ease, but can be for *words* only**
