import * as Chords from "../../main/typescript/main";
import { describe, expect, test } from '@jest/globals';

describe("chords tests", () => {
    test("hi", () => {
        console.log(Chords.ITQuasar.Multiverse.NSSChorsJS);
        expect(1).toBe(0);
    });
});
