import NSSChordsJS from "../../main/typescript/main";
import { describe, expect, test } from '@jest/globals';

describe("chords tests", () => {
    test("hi", () => {
        console.log(NSSChordsJS);
        expect(1).toBe(0);
    });
});
