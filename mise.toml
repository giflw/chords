[tools]
python = "3.13"
java = "24"
watchexec = "latest"

[env]
_.python.venv = { path = ".venv", create = true }

[hooks]
postinstall = "scoop install jbake"


[tasks.jdev]
run = "jbake -b -s"

[tasks.pdev]
run = "python -m mkdocs serve"
